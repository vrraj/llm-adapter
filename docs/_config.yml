# --- Site Metadata ---
title: "LLM Adapter"
tagline: "Unified Python wrapper for OpenAI and Gemini"
description: "Provider-agnostic, registry-driven LLM adapter for text generation and embeddings with normalized outputs - includes an interactive test UI"
url: "https://vrraj.github.io"
baseurl: "/llm-adapter"

# --- Theme Configuration ---
theme: jekyll-theme-cayman
show_downloads: true  

# --- Google Analytics (GA4) ---

google_analytics: G-YMXDNS4Y0L

# --- Plugins ---
plugins:
  - jekyll-seo-tag
  - jekyll-gist

# --- SEO Specifics ---
#  GitHub profile : vrraj/llm-adapter


# --- Build Settings ---
markdown: kramdown
highlighter: rouge
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor
